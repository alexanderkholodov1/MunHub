# NNNN — {{Title}}   *(AFLEK template — install as `specs/NNNN-slug/spec.md`)*

<!--
  TEMPLATE NOTES — delete this comment block when instantiating.
  - NNNN: zero-padded sequence; the specs/ folders are the canonical numbering (do not keep a
    parallel numbering scheme in a backlog — that is how numbering diverges).
  - A spec is the contract a work package points at. Acceptance criteria must be mechanically
    checkable wherever possible (a command, a grep, a test name — not an adjective).
  - Status values: draft → approved → in-progress → done → superseded.
-->

- **Status:** draft
- **Responsible:** <!-- executor tier or agent, e.g. "mid-tier implementer" -->
- **Depends on:** <!-- spec numbers, decisions (D-numbers), merged PRs -->
- **Phase:** <!-- project phase / epic / backlog reference -->

## Context

<!-- Why this exists; what architectural decision or user need it serves; what it deliberately
     is NOT (one paragraph each). -->

## Functional requirements

- FR1:
- FR2:

## Non-functional requirements

<!-- Performance, typing/strictness, purity (e.g. "no I/O in this package"), security. -->

## Design / approach

<!-- Files to create/touch and their responsibilities. Keep it concrete enough that the
     executor never has to invent structure. -->

## Acceptance criteria (verifiable)

- [ ] CA1:
- [ ] CA2:
- [ ] CA-N: build/test/lint/typecheck green
- [ ] CA-N+1: only the files/packages this spec lists are touched

## Out of scope

<!-- Adjacent work that someone WILL be tempted to do here, with the spec that owns it. -->

## Tasks

- [ ] T1:
- [ ] T2:
- [ ] T-N: update affected docs per the AGENTS.md documentation matrix
- [ ] T-N+1: changelog fragment
