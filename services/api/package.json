{
  "name": "@munhub/api",
  "version": "0.1.0",
  "private": true,
  "description": "MunHub backend / edge functions (Phase B): ingest, aggregations, background jobs",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "echo 'api build stub — wired in Phase B (F5+)'",
    "test": "vitest run",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "*",
    "vitest": "*"
  },
  "dependencies": {
    "@munhub/shared": "workspace:*",
    "@munhub/data-provider": "workspace:*",
    "@munhub/physics": "workspace:*"
  }
}
