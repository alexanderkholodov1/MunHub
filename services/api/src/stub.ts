/**
 * services/api — MunHub backend service stub (Phase B).
 *
 * Backend / edge functions for data ingest, aggregations, background jobs,
 * and admin APIs land in Phase B (F5+) specs.
 *
 * Phase A uses Firebase Realtime Database rules directly (no custom backend).
 */

export const API_STUB = "api-service-stub-v6" as const;
